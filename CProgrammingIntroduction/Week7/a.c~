#include<stdio.h>
#include<math.h>
main()
{
  int n,i;
  printf("nhap n");scanf("%d",&n)
  u= sqrt(n);
  for (i=1;i<=u;i++) {if n%u=0 printf("%d la so nguyen to",n);else printf("%d k la so nguyen to",n);}
}
